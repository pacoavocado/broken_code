<div class="text-center">
  <h2>HI</h2>
 {{#each tours as |tour|}}
  <div class="bio-cell">
  <h2>{{venue}}</h2> 
  <p>{{location}}</p>
  <p>{{show_date}}</p>
  <p>{{description}}</p>
  <h2>{{tour.venue}}</h2> 
  <p>{{tour.location}}</p>
  <p> {{tour.show_date}}</p>
  <p>{{tour.description}}</p>
  </div>
{{/each}}  
 <aside class="tourPic" >
   <img src ="./image/tourImg.jpg" alt = "brandImg">
 </aside>
</div>
